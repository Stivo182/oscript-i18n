
#Использовать i18n

Процедура ПодключитьКаталогРесурсов()

	ПутьКРесурсам = ОбъединитьПути(ТекущийСценарий().Каталог, "locales");
	МенеджерРесурсовЛокализации.ДобавитьКаталог(ПутьКРесурсам);	

КонецПроцедуры

Функция ТекстШаблона()

	Возврат "{
	|	'ОписаниеПриложения': '{t(ОписаниеПриложения)}',
	|	'Команды': {
	|		'Старт': {
	|			'Имя': '{t(Имя.Старт)}',
	|			'Описание': '{t(Описание.Старт)}'
	|		},
	|		'Стоп': {
	|			'Имя': '{t(Имя.Стоп)}',
	|			'Описание': '{t(Описание.Стоп)}'
	|		}
	|	}
	|}";

КонецФункции

ПодключитьКаталогРесурсов();

// ru
Пакет = МенеджерРесурсовЛокализации.ПолучитьПакет("РесурсыКонсольногоПриложения");

Шаблон = ТекстШаблона();
Пакет.ЗаполнитьШаблон(Шаблон);

Сообщить("Локаль: ru");
Сообщить(Пакет.ПолучитьРесурс("ОписаниеПриложения"));
Сообщить(Шаблон);

// en
Мультиязычность.УстановитьЛокаль("en");
Пакет = МенеджерРесурсовЛокализации.ПолучитьПакет("РесурсыКонсольногоПриложения");

Шаблон = ТекстШаблона();
Пакет.ЗаполнитьШаблон(Шаблон);

Сообщить("Локаль: en");
Сообщить(Пакет.ПолучитьРесурс("ОписаниеПриложения"));
Сообщить(Шаблон);